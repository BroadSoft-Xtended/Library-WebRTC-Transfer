.bdsft-client
	.transferPopup
		color white
		width 280px
		height 90px
		.target
			width 200px
		.actions
			padding 10px
		.reject
			color red

	.transferPopup.enableTransfer.started.transfer-shown
		{fadeInPopup}

	.transferPopup.enableTransfer.not(.transfer-shown)
		{fadeOut}
